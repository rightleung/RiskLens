# Credit Analyst Toolkit — Dependencies
numpy==2.4.2
pandas==3.0.1
yfinance==1.3.0
reportlab>=4.0,<5

fastapi==0.133.1
uvicorn==0.51.0
pydantic==2.12.5
pydantic-settings>=2.7

# Optional: install with `pip install -e ".[cn]"` if you need OpenCC-based zh-CN/zh-TW conversion.
# Optional: install with `pip install -e ".[cn-data]"` if you need AKShare-backed data.

# Testing
pytest==9.0.3
httpx==0.28.1

# Error monitoring
sentry-sdk==2.53.0
